# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.9.2            # 2.9.x provides prebuilt cp313 wheels
pydantic-settings==2.1.0
python-multipart==0.0.6

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0
beautifulsoup4==4.12.2
chardet==5.2.0
ebooklib==0.18

# Spiking Brain-Inspired LLM (SpikingBrain-7B)
torch==2.7.1              # Upgraded for SpikingBrain compatibility
sentence_transformers==2.7.0
transformers==4.55.2      # Upgraded for SpikingBrain compatibility
triton==3.3.1 ; platform_system != "Windows"
flash_attn==2.7.3 ; platform_system != "Windows"
flash-linear-attention==0.1 ; platform_system != "Windows"
vllm==0.10.0 ; platform_system != "Windows"
modelscope==1.20.1        ; platform_system != "Windows"  # For downloading SpikingBrain models
accelerate==1.1.1         # For model optimization

# Vector stores
chromadb==0.4.22
faiss-cpu==1.7.4 ; platform_system != "Windows"
faiss-cpu==1.12.0 ; platform_system == "Windows"

# LLM Integration
openai==1.6.1
langchain==0.1.0
langchain-community==0.0.13
tiktoken==0.7.0 ; platform_system != "Windows"  # optional; requires Rust toolchain on Windows
ollama==0.2.1
aiohttp==3.9.1  # Async HTTP client for web search

# Database
sqlalchemy==2.0.36
alembic==1.13.1
psycopg2-binary==2.9.9 ; platform_system != "Windows"
psycopg[binary]==3.2.12 ; platform_system == "Windows"

# Caching
redis==5.0.1
diskcache==5.6.3

# Testing
pytest==7.4.4
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.27.0

# Monitoring & Logging
loguru==0.7.2
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.39.1

# Utils
python-dotenv==1.0.0
click==8.1.7
tqdm==4.66.1
pyyaml==6.0.1
pathlib2==2.3.7
aiofiles==23.2.1
requests==2.31.0

# Development tools
black==23.12.1
isort==5.13.2
flake8==6.1.0
mypy==1.8.0
pre-commit==3.6.0
ruff==0.1.9

# Type hints
typing-extensions==4.12.2
